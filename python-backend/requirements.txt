# Guitar2Tabs 기본 의존성 (CPU, torch 없이 동작).
# 다성 채보/음원분리를 켜려면 requirements-hq.txt 를 추가로 설치한다.

# --- API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
python-multipart>=0.0.12
requests>=2.32

# --- 유튜브 추출 ---
yt-dlp>=2026.03.17
imageio-ffmpeg>=0.5.1

# --- 오디오 신호처리 ---
librosa>=0.10.2,<0.12
numpy>=1.26,<2.2
scipy>=1.11
soundfile>=0.12.1

# --- 악보 내보내기 ---
pretty_midi>=0.2.10
mido>=1.3.0
